# Quick Start Guide 🚀

## Швидкий старт проєкту

### Використовуючи Docker (найпростіший спосіб)

```bash
# Запустити все разом
docker-compose up --build

# Після успішного запуску відкрийте браузер:
# http://localhost:5173
```

### Локальна розробка

#### Крок 1: Backend

```bash
cd backend

# Активувати віртуальне середовище (якщо вже створено)
source venv/bin/activate  # Linux/Mac
# або створити нове
python -m venv venv && source venv/bin/activate

# Встановити залежності
pip install -r requirements.txt

# Запустити міграції
python manage.py migrate

# Запустити сервер
python manage.py runserver
```

Тримайте цей термінал відкритим!

#### Крок 2: Frontend (в новому терміналі)

```bash
cd frontend

# Встановити залежності (тільки перший раз)
npm install

# Запустити dev сервер
npm run dev
```

#### Крок 3: Відкрити додаток

Відкрийте в браузері: **http://localhost:5173**

### Перевірка з'єднання

```bash
# З кореневої папки проєкту
./check-connection.sh
```

## Що було зроблено для з'єднання

### ✅ Backend налаштування:
- CORS для `localhost:5173`
- CSRF токени з правильними cookie
- Session authentication
- REST API endpoints на `/api/*`

### ✅ Frontend налаштування:
- CSRF token handling
- Cookies передаються з кожним запитом
- Vite proxy для API запитів
- Всі endpoints оновлені для відповідності backend

### ✅ Що потрібно перевірити:

1. **Google OAuth** - додайте свої credentials в `backend/.env`:
   ```env
   GOOGLE_CLIENT_ID=your-client-id
   GOOGLE_CLIENT_SECRET=your-client-secret
   ```

2. **OpenAI API** (опціонально для генерації CV):
   ```env
   LLM_API_KEY=your-openai-api-key
   ```

## Структура проєкту

```
project-repo/
├── backend/           # Django REST API
│   ├── authz/        # Аутентифікація
│   ├── profiles/     # Профілі та CV
│   ├── interview/    # Інтерв'ю система
│   ├── trainer/      # Тренажер навичок
│   └── config/       # Налаштування Django
├── frontend/         # React + Vite
│   └── src/
│       ├── services/ # API клієнти (оновлено!)
│       ├── pages/    # Сторінки додатку
│       └── components/ # React компоненти
└── docker-compose.yml # Docker конфігурація
```

## Типові проблеми

### Backend не запускається
```bash
# Перевірте чи встановлені залежності
pip list

# Перевірте базу даних
python manage.py migrate
```

### Frontend не запускається
```bash
# Видаліть node_modules і встановіть знову
rm -rf node_modules package-lock.json
npm install
```

### CORS помилки
Переконайтесь що:
1. Backend запущений на порті 8000
2. Frontend запущений на порті 5173
3. В `backend/.env` є: `CORS_ALLOWED_ORIGINS=http://localhost:5173`

## Наступні кроки

1. **Налаштуйте Google OAuth** - щоб працювала аутентифікація
2. **Додайте OpenAI API key** - для генерації CV (опціонально)
3. **Створіть суперюзера** - для доступу до Django Admin:
   ```bash
   cd backend
   python manage.py createsuperuser
   ```

## Детальна документація

Дивіться `SETUP.md` для повної документації з:
- Детальними налаштуваннями
- Списком всіх API endpoints
- Інструкціями з тестування
- Production deployment guides
- Troubleshooting

---

**Потрібна допомога?** Перевірте логи:
- Backend: дивіться вивід в терміналі де запущений `runserver`
- Frontend: дивіться консоль браузера (F12)

